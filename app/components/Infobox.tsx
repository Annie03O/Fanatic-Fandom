"use client"
import { Show } from "../models/types/Show"


export const Infobox = ({type, shows}: {type: "Show" | "Character" | "Season" | "Episode" | "Actor", show: Show}) => {
    return (
        <section>
            
        </section>
    )
}